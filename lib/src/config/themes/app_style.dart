import 'package:flutter/material.dart';

abstract class AppStyles {
  static const header = TextStyle(fontSize: 16, fontWeight: FontWeight.bold);
  static const body = TextStyle();
  static const small = TextStyle(fontSize: 10);
}
